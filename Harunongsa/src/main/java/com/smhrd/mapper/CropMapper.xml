<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace = "org.mybatis.example.CropMapper">
	<select id = "allCrop" resultType="CropDTO">
		select cropidx, cropname, image, feature, sowingperiod, sowingmethod, cultivationmethod, harvestperiod, diseasepestinfo, recipe 
		from crops
	</select>
</mapper>